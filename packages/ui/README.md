# @parcferme/ui (placeholder)

Optional shared design system between this desktop app and the parcferme.cc web
app. Empty by design at M0 — populated only if/when the desktop UI grows enough
to warrant sharing components with the web team's React + Tailwind stack
(Build Plan §3).
